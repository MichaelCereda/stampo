name: Tests and release
on: [push, pull_request]
env:
  CRATE_NAME: ring-cli
  GITHUB_TOKEN: ${{ secrets.CICD_GITHUB_TOKEN }}
  RUST_BACKTRACE: 1
jobs:
  test:
    name: ${{ matrix.platform.os_name }} with rust ${{ matrix.toolchain }}
    runs-on: ${{ matrix.platform.os }}
    strategy:
      fail-fast: false
      matrix:
        platform:

          - os_name: FreeBSD-x86_64
            os: ubuntu-latest
            target: x86_64-unknown-freebsd
            bin: ring-cli
            name: ring-cli-FreeBSD-x86_64.tar.gz
            skip_tests: true
          - os_name: Linux-x86_64
            os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            bin: ring-cli
            name: ring-cli-Linux-x86_64-musl.tar.gz
          - os_name: Linux-aarch64
            os: ubuntu-latest
            target: aarch64-unknown-linux-musl
            bin: ring-cli
            name: ring-cli-Linux-aarch64-musl.tar.gz
          - os_name: Linux-arm
            os: ubuntu-latest
            target: arm-unknown-linux-musleabi
            bin: ring-cli
            name: ring-cli-Linux-arm-musl.tar.gz
          - os_name: Linux-i686
            os: ubuntu-latest
            target: i686-unknown-linux-musl
            bin: ring-cli
            name: ring-cli-Linux-i686-musl.tar.gz
            skip_tests: true
          - os_name: Linux-mips
            os: ubuntu-latest
            target: mips-unknown-linux-musl
            bin: ring-cli
            name: ring-cli-Linux-mips.tar.gz
          - os_name: Linux-mipsel
            os: ubuntu-latest
            target: mipsel-unknown-linux-musl
            bin: ring-cli
            name: ring-cli-Linux-mipsel.tar.gz
          - os_name: Linux-mips64
            os: ubuntu-latest
            target: mips64-unknown-linux-muslabi64
            bin: ring-cli
            name: ring-cli-Linux-mips64.tar.gz
            skip_tests: true
          - os_name: Linux-mips64el
            os: ubuntu-latest
            target: mips64el-unknown-linux-muslabi64
            bin: ring-cli
            name: ring-cli-Linux-mips64el.tar.gz
            skip_tests: true
          - os_name: Linux-powerpc
            os: ubuntu-latest
            target: powerpc-unknown-linux-gnu
            bin: ring-cli
            name: ring-cli-Linux-powerpc-gnu.tar.gz
            skip_tests: true
          - os_name: Linux-powerpc64
            os: ubuntu-latest
            target: powerpc64-unknown-linux-gnu
            bin: ring-cli
            name: ring-cli-Linux-powerpc64-gnu.tar.gz
            skip_tests: true
          - os_name: Linux-powerpc64le
            os: ubuntu-latest
            target: powerpc64le-unknown-linux-gnu
            bin: ring-cli
            name: ring-cli-Linux-powerpc64le.tar.gz
            skip_tests: true
          - os_name: Linux-riscv64
            os: ubuntu-latest
            target: riscv64gc-unknown-linux-gnu
            bin: ring-cli
            name: ring-cli-Linux-riscv64gc-gnu.tar.gz
          - os_name: Linux-s390x
            os: ubuntu-latest
            target: s390x-unknown-linux-gnu
            bin: ring-cli
            name: ring-cli-Linux-s390x-gnu.tar.gz
            skip_tests: true
          - os_name: NetBSD-x86_64
            os: ubuntu-latest
            target: x86_64-unknown-netbsd
            bin: ring-cli
            name: ring-cli-NetBSD-x86_64.tar.gz
            skip_tests: true
          - os_name: Windows-aarch64
            os: windows-latest
            target: aarch64-pc-windows-msvc
            bin: ring-cli.exe
            name: ring-cli-Windows-aarch64.zip
            skip_tests: true
          - os_name: Windows-i686
            os: windows-latest
            target: i686-pc-windows-msvc
            bin: ring-cli.exe
            name: ring-cli-Windows-i686.zip
            skip_tests: true
          - os_name: Windows-x86_64
            os: windows-latest
            target: x86_64-pc-windows-msvc
            bin: ring-cli.exe
            name: ring-cli-Windows-x86_64.zip
          - os_name: macOS-x86_64
            os: macOS-latest
            target: x86_64-apple-darwin
            bin: ring-cli
            name: ring-cli-Darwin-x86_64.tar.gz
          - os_name: macOS-aarch64
            os: macOS-latest
            target: aarch64-apple-darwin
            bin: ring-cli
            name: ring-cli-Darwin-aarch64.tar.gz
            skip_tests: true
        toolchain:
          - stable
    steps:
      - uses: actions/checkout@v4
      - name: Cache cargo & target directories
        uses: Swatinem/rust-cache@v2
        with:
          key: "v2"
      - name: Configure Git
        run: |
          git config --global user.email "jdoe@example.com"
          git config --global user.name "J. Doe"
      - name: Install musl-tools on Linux
        run: sudo apt-get update --yes && sudo apt-get install --yes musl-tools
        if: contains(matrix.platform.name, 'musl')
      - name: Build binary
        uses: houseabsolute/actions-rust-cross@v0
        with:
          command: "build"
          target: ${{ matrix.platform.target }}
          toolchain: ${{ matrix.toolchain }}
          args: "--locked --release"
          strip: true
      - name: Run tests
        uses: houseabsolute/actions-rust-cross@v0
        with:
          command: "test"
          target: ${{ matrix.platform.target }}
          toolchain: ${{ matrix.toolchain }}
          args: "--locked --release"
        if: ${{ !matrix.platform.skip_tests }}
      - name: Package as archive
        shell: bash
        run: |
          cd target/${{ matrix.platform.target }}/release
          if [[ "${{ matrix.platform.os }}" == "windows-latest" ]]; then
            7z a ../../../${{ matrix.platform.name }} ${{ matrix.platform.bin }}
          else
            tar czvf ../../../${{ matrix.platform.name }} ${{ matrix.platform.bin }}
          fi
          cd -
        if: |
          matrix.toolchain == 'stable' &&
          ( startsWith( github.ref, 'refs/tags/v' ) ||
            github.ref == 'refs/tags/test-release' )
      - name: Publish release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ring-cli-${{ matrix.platform.os_name }}
          path: "ring-cli-*"
        if: matrix.toolchain == 'stable' && github.ref == 'refs/tags/test-release'
      - name: Generate SHA-256
        run: shasum -a 256 ${{ matrix.platform.name }}
        if: |
          matrix.toolchain == 'stable' &&
          matrix.platform.os == 'macOS-latest' &&
          ( startsWith( github.ref, 'refs/tags/v' ) ||
            github.ref == 'refs/tags/test-release' )
      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          files: "ring-cli*"
        if: matrix.toolchain == 'stable' && startsWith( github.ref, 'refs/tags/v' )
