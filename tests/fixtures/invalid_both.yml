version: "2.0"
description: "Invalid config"
commands:
  bad:
    description: "Has both cmd and subcommands"
    flags: []
    cmd:
      run:
        - "echo hello"
    subcommands:
      sub:
        description: "A subcommand"
        flags: []
        cmd:
          run:
            - "echo sub"
