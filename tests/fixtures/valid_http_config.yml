version: "2.0"
description: "HTTP Test CLI"
commands:
  fetch:
    description: "Fetch a URL"
    flags:
      - name: "url"
        description: "URL to fetch"
    cmd:
      http:
        method: "GET"
        url: "${{url}}"
